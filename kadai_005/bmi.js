// 変数の宣言
let bodyWeightKG;
let bodyHeightM;
let bmi;

// 値の代入
bodyWeightKG = 68;
bodyHeightM = 1.7;

// BMIの算出
bmi = bodyWeightKG / (bodyHeightM * bodyHeightM);

// コンソールへの出力
console.log(bmi);
